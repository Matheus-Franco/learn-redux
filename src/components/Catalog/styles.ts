import styled from 'styled-components';

export const Container = styled.div`
  width: 1120px;
  margin: 0 auto;

  padding: 30px 0;

  main {
    display: flex;
    flex-direction: row;
    justify-content: space-between;
  }
`;
